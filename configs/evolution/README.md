Evolution configs live here.

Current active file:

- `default.json`: the default base config used by `python -m scripts.run_evolution`

You can override it with:

```bash
python -m scripts.run_evolution --config configs/evolution/default.json
```
