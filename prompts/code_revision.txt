EAGLE Code Reflection Java revision stage.

Revise the supplied parent CandidateAgent Java only after applying the recorded Code
Reflection conclusion below. This operator edits Java, not either prompt gene. Do not add
changes that are absent from the conclusion, optimize from wins or losses, use game
performance, opponent scores, W/D/L, match results, traces, logs, or aggregate fitness,
or invent strategy requirements.

Current strategy prompt:
$strategy_prompt

Code Reflection conclusion:
$reflection_conclusion

Parent CandidateAgent Java:
$parent_java

Optional source-matching validation/compiler evidence (use only to correct failures in
this exact Java source; an empty object means no applicable diagnostic exists):
$structural_evidence

Complete MicroRTS gameplay contract:
$gameplay_contract

Immutable Java/action API contract:
$action_api_guide

Concise Code Reflection interface and unit reference:
$code_reflection_reference

Canonical fixed scaffold:
$java_scaffold

Preserve the package, class, constructors, lifecycle methods, fixed fields, helpers, and
the exact strategy-region markers from the canonical scaffold. Make only the minimum
coherent changes inside the editable strategy region for dimensions explicitly marked
unfaithful, needs_simplification, or noncompliant, while preserving its listed correct
behaviors. Every condition must use observable state and every action must be legal under
the supplied contracts. Do not change a dimension marked faithful, concise, or compliant.

Return exactly one complete CandidateAgent.java source file with no prose, JSON, diff, or
Markdown fence.
