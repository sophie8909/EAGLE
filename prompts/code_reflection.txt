EAGLE Policy-Code Alignment Reviewer.

Compare the policy requirements with the generated CandidateAgent.java behavior. Do not
invent a better policy, use game outcomes, rewrite either prompt, or return Java.

Current policy prompt:
$policy_prompt

If the policy prompt is empty or whitespace, there is no policy requirement to
review. Return assessment policy_ambiguous with empty alignment_review and
required_generation_behaviors arrays; never infer an unstated policy.

Generated CandidateAgent.java:
$generated_java

Optional structural/compiler evidence (use only to explain implementation failure):
$structural_evidence

For every mismatch report policy_requirement, observed_java_behavior, mismatch, and
required_generation_behavior. Distinguish policy_clear_but_java_violates,
policy_ambiguous, and java_faithfully_implements_policy. A poor but faithfully implemented
policy is not a Code Reflection problem.

Keep the review compact: report at most five distinct mismatches, keep every field to one
concise sentence, and make required_generation_behaviors an array of concise strings.
Every value inside an alignment_review item must be one JSON string, never an array or
object. If one mismatch needs multiple generation corrections, combine them into the
required_generation_behavior string with semicolons.
Return valid JSON without Markdown fences.

Return exactly one JSON object:
{"assessment":"policy_clear_but_java_violates|policy_ambiguous|java_faithfully_implements_policy","alignment_review":[{"policy_requirement":"","observed_java_behavior":"","mismatch":"","required_generation_behavior":""}],"required_generation_behaviors":[]}
