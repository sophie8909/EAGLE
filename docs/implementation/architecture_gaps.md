# Architecture gaps

The normative requirement is always [`../eagle_architecture_spec.md`](../eagle_architecture_spec.md). `Open` means implementation differs from that contract. `Partial` means a scoped foundation is active but the complete contract remains for a later phase. `Implemented` means current code, focused tests, and required artifacts agree. `Resolved decision` means the normative choice is recorded, but dependent implementation gaps remain open until code, tests, configuration, and artifacts conform.

| Contract | Required behavior | Current behavior | Affected files | Required change | Status |
| --- | --- | --- | --- | --- | --- |
| G-01 Candidate schema | First-class identity, genotype, phenotype, variation, provenance, failure, objectives, artifacts, and timing; pre/post Java both reconstructable. | Candidate fields are first-class; evaluation preserves the input genotype and stores normalized output as `generated_java`; canonical genotype/phenotype files round-trip separately. | `eagle/candidate.py`, `eagle/evaluation.py`, `eagle/artifacts.py` | Completed by Migration Phase 1 with focused state-transition and artifact tests. | Implemented (2026-07-14) |
| G-02 Crossover provenance | Independent component selection from latest evaluated parent Java with three source IDs. | Uniform Crossover reads `parent.generated_java`, records all three source IDs immediately, and search resolves feedback from provenance rather than string equality. | `eagle/crossover.py`, `eagle/search.py` | Completed by Migration Phase 1 with seeded, all-eight-combination, and equal-text tests. | Implemented (2026-07-14) |
| G-03 Mutation calls | Reflection LLM, Rewrite LLM, then final Java Generation LLM; all calls logged. | Phase 2A has configured Reflection LLM transport, bounded retries, per-attempt logging, and no prompt rewrite yet. | `eagle/mutation.py`, `eagle/search.py`, `generation/backend.py`, `eagle/llm_logging.py` | Wire Rewrite and final-generation mutation stages, parsing, retries, evidence inputs, logging, and failure retention. | Partial (Phase 2A) |
| G-04 Mutation evidence | Full 10-match/behavior evidence for Strategy; full generation/validation/compile/integration/runtime evidence for Code. | Typed Reflection context and complete prompt fields are active; later evaluation stages still provide only currently available repository evidence. | `eagle/mutation.py`, `eagle/search.py` | Build typed feedback payloads from the later complete evaluation artifact contract. | Partial (Reflection foundation) |
| G-05 Evaluation protocol | Compile once and run exactly 10 LightRush matches without regeneration. | Loop reuses one compile, but count defaults to 1 and checked-in configs use 1/3. | `eagle/config.py`, `configs/*.yaml`, `eagle/evaluation.py` | Enforce 10 for architecture runs, update configs, and test call/hash invariants. | Open |
| G-06 Game Performance | Bounded per-match shaping and 10-match mean from the canonical formula. | Unbounded state/resource terms plus unconditional survival weight 200. | `evaluation/game_performance.py`, `evaluation/game_metrics.py`, configs, tests | Replace formula, centralize scales/material values, persist components/version, and update tests. | Open |
| G-07 successful Code Quality | Selected `+500` base + warning component + capability score + independent alignment LLM; range `[0, 610]`. | Compile + marked-region validity + deterministic static text score; no alignment call. | `evaluation/code_quality.py`, `eagle/evaluation.py`, tests | Implement and version the selected formula and remove fixed-region fitness dependency. | Open |
| G-08 Failure hierarchy | Distinct generation, validation, compilation, integration, runtime scores. | Categories are coarse; compilation can total `-1100`; no distinct integration stage or progress scores. | `generation/java_agent_generator.py`, `eagle/evaluation.py`, `evaluation/code_quality.py`, `evaluation/nsga2_objectives.py` | Add typed stages/checks and canonical failure formulas. | Open |
| G-09 Runtime-contract validation | Validate exact `ai.generated.CandidateAgent` identity, both required constructors, required AI methods, and security contract without fixed internal structure. | Requires exact `AbstractionLayerAI` declaration, strategy/action markers, six helper methods, and fixed order; does not enforce the complete resolved external contract. | `generation/agent_template.py`, `generation/java_agent_generator.py`, Java template tests | Separate optional seed-template checks from candidate runtime validation and implement the resolved external/security contract. | Open |
| G-10 Compiler diagnostics | `-Xlint`, structured/deduplicated warnings/errors, capped warning penalty. | No warning flag; substring line counting; no deduplication; penalty uncapped in current formula. | `evaluation/compiler.py`, `evaluation/code_quality.py` | Add diagnostic flags/parser, persistence, and canonical scoring tests. | Open |
| G-11 Integration and runtime execution | Seven ordered load/type/two-constructor/reset/clone/getAction/PlayerAction checks before matches; match seeds/timeouts and precise partial-batch classification. | First process run combines integration/runtime; no seed argument or wall timeout; fixed map. | `evaluation/microrts_runner.py`, `eagle/evaluation.py` | Add the resolved seven-check integration adapter, seeds, timeout, result validation, and stage routing. | Open |
| G-12 Artifact schema | Canonical, versioned run/generation/candidate/stage/match layout with raw mutation/generation data. | Phase 1 writes versioned lineage, canonical genotype/normalized phenotype paths, crossover provenance, and resolved config; remaining stage/match outputs and summaries retain transitional flat/duplicate paths. | `eagle/artifacts.py`, `eagle/search.py`, runner/logging | Complete stage/match serializers, hashes, atomic summaries, readers/migrations, and duplicate removal in the later artifact phase. | Partial (Phase 1 foundation) |
| G-13 Timing schema | Candidate/stage/attempt/match timestamps and durations. | Reflection stage writes UTC attempt timing and candidate timing; Rewrite, Generation, validation, integration, and match timing remain incomplete. | `eagle/llm_logging.py`, orchestration, artifacts, runner | Instrument Rewrite, Generation, validation, integration, and match stages with UTC timestamps and monotonic durations. | Partial (Reflection foundation) |
| G-14 Lineage schema | Stable operator, mutation type, parents, three component sources, source candidates. | First-class fields serialize to versioned `lineage.json` for every seed, copy, crossover, mutation, and crossover-plus-mutation candidate; metadata is not required for reconstruction. | `eagle/candidate.py`, crossover/mutation/search/artifacts | Completed by Migration Phase 1 lineage/provenance fixtures. | Implemented (2026-07-14) |
| G-15 Resolved configuration | Persist actual runtime values, seeds, versions, prompt/model/retry data, Git commit. | Every run writes actual parsed/defaulted/forced/mock-overridden values, map/cycles, EA and LLM settings, retry policy, schema/formula identifiers, Git commit, and explicit null/unsupported fields for unavailable prompt version and match seeds. | `eagle/config.py`, `eagle/search.py`, `eagle/artifacts.py` | Completed by Migration Phase 1 resolved-value tests. | Implemented (2026-07-14) |
| G-16 Strategy Alignment objective boundary | Alignment is a 0–10 `code_quality` component only. | Active optimizer uses two correct names, but alignment is absent; legacy readers/YAML/UI text retain old surfaces. | `evaluation/code_quality.py`, `scripts/analyze_run.py`, configs, CLI help, recent artifacts | Implement component, preserve legacy reads only behind explicit schema migration, remove stale active config/help. | Open |
| G-17 Operations and tests | Commands/schema readers and tests prove the current contract. | Mock smoke proves current implementation; GUI discovery is transitional; many tests enforce non-normative marker/static formulas. | `scripts/`, `tests/` | Update after foundational schemas/operators; add contract matrix and current real integration smoke. | Open |
| G-18 Legacy residue | Historical split/function-body, surrogate, runtime-LLM, RandomAI/one-match concepts cannot guide active work. | Old runs and compatibility code remain readable; old docs previously promoted these concepts. | ignored runs/logs, `scripts/analyze_run.py`, GUI discovery | Fence legacy readers by schema/version and remove unused compatibility after migration window. | Open |
| A-01 Successful `code_quality` base | Use `500 + compilation_score + function_score + strategy_alignment_score`; range `[0, 610]`. | Normative decision recorded; resolved config truthfully identifies the active legacy formula, while scoring still uses that legacy deterministic implementation. | spec sections 17 and 29; code-quality docs/implementation | Implement the selected formula through G-07, then advance ART-09 from the legacy identifier to the canonical version. | Resolved decision (2026-07-14); implementation open |
| A-02 Exact Java identity/integration checks | Require package `ai.generated`, public `CandidateAgent`, superclass `AbstractionLayerAI`, both named constructors, required `getAction`/`reset`/`clone`, and seven ordered pre-match checks. | Normative decision recorded; current validator remains marker/helper-based and no distinct integration stage exists. | spec sections 12, 16.5, and 29; generation/integration modules | Implement the resolved validation/integration contracts through G-09 and G-11. | Resolved decision (2026-07-14); implementation open |

## Gap update rule

Close an implementation gap only when implementation, tests, configuration, artifacts, and affected canonical docs agree. A resolved architecture decision does not close its dependent implementation gaps. Record the validating test or schema version in the status text. Do not mark a gap closed from an old run artifact.
