# Generated by scripts/llama_launcher.py on each machine.
# This example is intentionally incomplete and is not a runnable endpoint config.
# The launcher writes the real coder LAN address and preserves the other section.

[general]
profile = "general"
base_url = "http://127.0.0.1:8080/v1"
model = "qwen3.5-9b"

# [coder] is created by the coder launcher after it detects Machine A's private LAN IP.
