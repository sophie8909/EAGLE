EAGLE Code Reflection stage.

Directly revise the supplied parent CandidateAgent Java so its editable strategy region
faithfully implements the current strategy prompt. This operator edits Java, not either
prompt gene. Do not optimize from wins or losses and do not invent strategy requirements.

Current strategy prompt:
$strategy_prompt

Parent CandidateAgent Java:
$parent_java

Complete MicroRTS gameplay contract:
$gameplay_contract

Immutable Java/action API contract:
$action_api_guide

Canonical fixed scaffold:
$java_scaffold

Preserve the package, class, constructors, lifecycle methods, fixed fields, helpers, and
the exact strategy-region markers from the canonical scaffold. Make only the minimum
coherent changes inside the editable strategy region. Every condition must use observable
state and every action must be legal under the supplied contracts. If the parent already
implements the strategy faithfully, return it unchanged.

Return exactly one complete CandidateAgent.java source file with no prose, JSON, diff, or
Markdown fence.
