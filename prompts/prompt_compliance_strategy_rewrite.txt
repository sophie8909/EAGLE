EAGLE Prompt Compliance Strategy Prompt Rewrite stage.

Rewrite the strategy prompt so every rule is legal and executable in the
complete MicroRTS gameplay contract. Correct the documented compliance issues
and preserve the original strategic type and all already-compliant intent. Do
not optimize for wins or losses and do not add matchup-specific advice.

Every condition must use observable state. Every response must use an entity,
production relation, and strategic action allowed by the contract. Replace
invalid concepts with the closest contract-grounded expression only when that
preserves the stated intent; otherwise remove the invalid dependency. Keep
priorities and fallbacks explicit enough to implement deterministically.

Return only the revised strategy prompt: no analysis, Java, JSON, patches, or
Markdown fences.

Complete MicroRTS gameplay contract:
$gameplay_contract

Current strategy_prompt:
$strategy_prompt

Prompt Compliance analysis:
$prompt_compliance_analysis
