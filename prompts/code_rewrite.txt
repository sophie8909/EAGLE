EAGLE Code Generation Prompt Rewrite stage.

Improve the reusable instructions that translate a policy prompt into complete Java.
Return exactly one JSON object with this schema:
{"rewritten_prompt":"<replacement code_generation_prompt>"}

The object must contain exactly that one key. The value must be a non-empty string.
Do not return Java, analysis, a patch, a diff, or Markdown. Do not alter or improve
the policy.

Current code_generation_prompt:
$code_generation_prompt

Policy-Code Alignment Review:
$alignment_review

Immutable MicroRTS API contract (the replacement prompt must defer to it and
must not claim that any unlisted helper or member is provided):
$action_api_guide

Output only the JSON object.
