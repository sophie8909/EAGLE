EAGLE Rewrite validation retry.

Original request:
$original_request

Rejected response:
$previous_response

The previous response was rejected by deterministic validation:
$validation_error

Correct only the reported error while preserving valid content. Follow the
original contracts and exact output schema; return no explanation or Markdown.
