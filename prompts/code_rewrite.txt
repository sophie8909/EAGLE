EAGLE Code Generation Prompt Rewrite stage.

Improve the reusable rules that translate any policy prompt into complete Java. Return a
rule delta; runtime applies it deterministically to the current rules. Do not rewrite the
whole prompt.

Return exactly one JSON object with this schema:
{"remove_rule_ids":[],"add_rules":[{"category":"requirement_coverage|priority_ordering|role_assignment|state_continuity|targeting_fallback|policy_grounding|api_compliance","instruction":"<one reusable rule>"}]}

The object must contain exactly those two keys. remove_rule_ids may name only IDs from
Current reusable rules. Each add_rules item must contain exactly category and instruction.
Make at least one effective change and leave at least one rule after removals.

Every added instruction must be policy-agnostic and useful for unseen policies. Convert a
specific mismatch into a general translation principle such as preserving policy priority,
making stated conditions reachable, or maintaining mutually exclusive roles. Never copy or
paraphrase the current policy, name its concrete units/structures/strategy, prescribe helper
or variable names, include Java fragments, or request edits to the immutable scaffold. The
policy is supplied separately to the Generator and must not be embedded in this reusable
gene. Do not return Java, analysis, a patch, a diff, or Markdown.

Current code_generation_prompt:
$code_generation_prompt

Current reusable rules (canonical JSON; legacy free-form prompts have no reusable rules and
must be replaced through add_rules):
$current_reusable_rules

Policy-Code Alignment Review:
$alignment_review

Immutable MicroRTS API contract (the replacement prompt must defer to it and
must not copy its concrete helpers into reusable rules or claim that any unlisted helper or
member is provided):
$action_api_guide

Output only the JSON object.
