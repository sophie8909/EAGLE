EAGLE Prompt Compliance Code Generation Prompt Rewrite stage.

Repair the reported code-generation-prompt compliance problem by returning one
reusable rule delta. The runtime applies and validates the delta; do not rewrite
the whole prompt or add a game-playing strategy.

Return exactly one raw JSON object:
{"remove_rule_ids":[],"add_rules":[{"category":"requirement_coverage|priority_ordering|role_assignment|state_continuity|targeting_fallback|policy_grounding|api_compliance","instruction":"<one reusable rule>"}]}

Use exactly one add_rules item and at most one removal ID from Current reusable
rules. The instruction must be one policy-agnostic sentence of 12-240 normalized
characters. If 10 rules already exist, replace one. State a translation
invariant, not Java, an API/helper name, a concrete strategy/entity, a scaffold
edit, or an invented runtime mechanism. Preserve the idle-actor requirement.

Current code_generation_prompt:
$code_generation_prompt

Current reusable rules (valid array or invalid status with recoverable rules):
$current_reusable_rules

Prompt Compliance analysis:
$prompt_compliance_analysis

Complete MicroRTS gameplay contract:
$gameplay_contract

Immutable MicroRTS API contract:
$action_api_guide
